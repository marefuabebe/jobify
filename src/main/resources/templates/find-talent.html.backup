<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml" xmlns:th="http://www.thymeleaf.org"
    xmlns:sec="http://www.thymeleaf.org/extras/spring-security">

<head>
    <meta charset="UTF-8" />
    <title>Find Talent - Jobify</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <link rel="stylesheet" th:href="@{/webjars/bootstrap/css/bootstrap.min.css}" />
    <link rel="stylesheet" th:href="@{/webjars/font-awesome/css/all.min.css}" />
    <style>
        :root {
            --primary: #E54714;
            --primary-light: #FCE8E2;
            --primary-dark: #C43B0F;
            --dark: #1A1A2E;
            --light: #F8F9FA;
            --gray: #6C757D;
            --border: #E0E0E0;
            --radius: 8px;
            /* Increased from 6px */
            --shadow: 0 4px 16px rgba(229, 71, 20, 0.12);
            /* Enhanced shadow */
            --transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: var(--light);
            color: var(--dark);
            font-size: 15px;
            /* Increased from 14px */
            line-height: 1.5;
        }

        /* Header */
        .navbar {
            background: linear-gradient(135deg, var(--primary) 0%, var(--primary-dark) 100%);
            padding: 12px 0;
            /* Increased from 8px */
            box-shadow: 0 4px 16px rgba(229, 71, 20, 0.25);
            /* Enhanced shadow */
            animation: slideDown 0.5s ease-out;
        }

        .navbar-brand {
            font-weight: 700;
            font-size: 20px;
            /* Increased from 18px */
            color: white;
            padding-left: 16px;
            /* Increased from 12px */
            position: relative;
            overflow: hidden;
        }

        .navbar-brand::after {
            content: '';
            position: absolute;
            bottom: -2px;
            left: 16px;
            /* Increased from 12px */
            width: 0;
            height: 2px;
            background: white;
            transition: width 0.3s ease;
        }

        .navbar-brand:hover::after {
            width: 70px;
            /* Increased from 60px */
        }

        .navbar-nav .nav-link {
            color: rgba(255, 255, 255, 0.9);
            padding: 10px 16px;
            /* Increased from 8px 12px */
            font-size: 14px;
            /* Increased from 13px */
            font-weight: 500;
            border-radius: var(--radius);
            transition: var(--transition);
            position: relative;
            overflow: hidden;
            margin: 0 4px;
        }

        .navbar-nav .nav-link::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: rgba(255, 255, 255, 0.1);
            transition: left 0.3s ease;
        }

        .navbar-nav .nav-link:hover::before {
            left: 0;
        }

        .navbar-nav .nav-link:hover {
            background: rgba(255, 255, 255, 0.1);
            color: white;
            transform: translateY(-2px);
        }

        .navbar-nav .nav-link.active {
            background: rgba(255, 255, 255, 0.2);
            /* Increased opacity */
            color: white;
            transform: translateY(-2px);
            box-shadow: 0 2px 12px rgba(255, 255, 255, 0.2);
        }

        .badge {
            font-size: 12px;
            /* Increased from 11px */
            padding: 4px 10px;
            /* Increased from 3px 6px */
            font-weight: 600;
            transition: var(--transition);
        }

        .badge:hover {
            transform: scale(1.08);
            /* Slightly more scale */
        }

        .user-avatar {
            width: 40px;
            /* Increased from 36px */
            height: 40px;
            object-fit: cover;
            border: 2px solid rgba(255, 255, 255, 0.4);
            /* Increased opacity */
            transition: var(--transition);
            cursor: pointer;
        }

        .user-avatar:hover {
            transform: scale(1.12);
            /* Increased from 1.1 */
            border-color: white;
            box-shadow: 0 0 12px rgba(255, 255, 255, 0.4);
            /* Enhanced shadow */
        }

        .user-name {
            color: white;
            font-weight: 500;
            font-size: 14px;
            /* Increased from 13px */
            transition: var(--transition);
            padding: 0 12px;
        }

        .logout-btn {
            border: 2px solid rgba(255, 255, 255, 0.4);
            /* Increased from 1px */
            color: white;
            padding: 8px 20px;
            /* Increased from 6px 16px */
            font-size: 14px;
            /* Increased from 13px */
            border-radius: var(--radius);
            transition: var(--transition);
            position: relative;
            overflow: hidden;
            margin-left: 8px;
        }

        .logout-btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.3), transparent);
            /* Increased opacity */
            transition: left 0.5s ease;
        }

        .logout-btn:hover::before {
            left: 100%;
        }

        .logout-btn:hover {
            background: white;
            color: var(--primary);
            transform: translateY(-2px);
            box-shadow: 0 4px 16px rgba(255, 255, 255, 0.2);
        }

        /* Main Layout */
        .find-talent-container {
            display: flex;
            min-height: calc(100vh - 64px);
            /* Adjusted for increased navbar height */
            animation: fadeIn 0.6s ease-out;
        }

        /* Sidebar */
        .sidebar {
            width: 280px;
            /* Increased from 260px */
            background: white;
            border-right: 2px solid var(--border);
            /* Increased from 1px */
            padding: 20px;
            /* Increased from 16px */
            flex-shrink: 0;
            overflow-y: auto;
            max-height: calc(100vh - 64px);
            animation: slideInLeft 0.5s ease-out 0.2s both;
        }

        .sidebar-header {
            font-size: 13px;
            /* Increased from 12px */
            font-weight: 600;
            color: var(--gray);
            text-transform: uppercase;
            letter-spacing: 0.6px;
            /* Increased from 0.5px */
            margin-bottom: 20px;
            /* Increased from 16px */
            padding-bottom: 12px;
            /* Increased from 8px */
            border-bottom: 2px solid var(--border);
            /* Increased from 1px */
            position: relative;
        }

        .sidebar-header::after {
            content: '';
            position: absolute;
            bottom: -2px;
            /* Adjusted for border thickness */
            left: 0;
            width: 50px;
            /* Increased from 40px */
            height: 3px;
            /* Increased from 2px */
            background: var(--primary);
            animation: slideInRight 0.6s ease-out 0.4s both;
        }

        .filter-group {
            margin-bottom: 20px;
            /* Increased from 16px */
            animation: fadeInUp 0.4s ease-out;
        }

        .filter-label {
            font-size: 14px;
            /* Increased from 13px */
            font-weight: 600;
            color: var(--dark);
            margin-bottom: 8px;
            /* Increased from 6px */
            display: flex;
            align-items: center;
            gap: 8px;
            /* Increased from 6px */
        }

        .filter-label i {
            color: var(--primary);
            font-size: 14px;
            /* Increased from 12px */
        }

        .filter-input {
            width: 100%;
            padding: 10px 12px;
            /* Increased from 8px 10px */
            border: 2px solid var(--border);
            /* Increased from 1px */
            border-radius: var(--radius);
            font-size: 14px;
            /* Increased from 13px */
            transition: var(--transition);
        }

        .filter-input:focus {
            outline: none;
            border-color: var(--primary);
            box-shadow: 0 0 0 3px rgba(229, 71, 20, 0.15);
            /* Enhanced shadow */
            transform: translateY(-2px);
            /* Increased from -1px */
        }

        .filter-select {
            width: 100%;
            padding: 10px 12px;
            /* Increased from 8px 10px */
            border: 2px solid var(--border);
            /* Increased from 1px */
            border-radius: var(--radius);
            font-size: 14px;
            /* Increased from 13px */
            background: white;
            cursor: pointer;
            transition: var(--transition);
        }

        .filter-select:focus {
            outline: none;
            border-color: var(--primary);
            box-shadow: 0 0 0 3px rgba(229, 71, 20, 0.15);
            /* Enhanced shadow */
            transform: translateY(-2px);
            /* Increased from -1px */
        }

        .filter-btn {
            background: var(--primary);
            color: white;
            border: none;
            border-radius: var(--radius);
            padding: 12px 16px;
            /* Increased from 10px 12px */
            font-size: 14px;
            /* Increased from 13px */
            font-weight: 600;
            cursor: pointer;
            width: 100%;
            transition: var(--transition);
            position: relative;
            overflow: hidden;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
            /* Increased from 8px */
        }

        .filter-btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.25), transparent);
            /* Increased opacity */
            transition: left 0.5s ease;
        }

        .filter-btn:hover::before {
            left: 100%;
        }

        .filter-btn:hover {
            background: var(--primary-dark);
            transform: translateY(-3px);
            /* Increased from -2px */
            box-shadow: 0 6px 20px rgba(229, 71, 20, 0.4);
            /* Enhanced shadow */
        }

        .clear-btn {
            background: white;
            color: var(--dark);
            border: 2px solid var(--border);
            /* Increased from 1px */
            border-radius: var(--radius);
            padding: 12px 16px;
            /* Increased from 10px 12px */
            font-size: 14px;
            /* Increased from 13px */
            font-weight: 600;
            cursor: pointer;
            width: 100%;
            transition: var(--transition);
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
            /* Increased from 8px */
            margin-top: 12px;
            /* Increased from 8px */
        }

        .clear-btn:hover {
            background: var(--light);
            border-color: var(--primary);
            color: var(--primary);
            transform: translateY(-3px);
            /* Increased from -2px */
        }

        /* Main Content */
        .main-content {
            flex: 1;
            padding: 24px;
            /* Increased from 16px */
            overflow-y: auto;
            max-height: calc(100vh - 64px);
        }

        .main-content::-webkit-scrollbar {
            width: 6px;
            /* Increased from 4px */
        }

        .main-content::-webkit-scrollbar-track {
            background: #f1f1f1;
            border-radius: 3px;
        }

        .main-content::-webkit-scrollbar-thumb {
            background: var(--primary);
            border-radius: 3px;
            /* Increased from 2px */
        }

        .main-content::-webkit-scrollbar-thumb:hover {
            background: var(--primary-dark);
        }

        /* Page Header */
        .page-header {
            margin-bottom: 24px;
            /* Increased from 20px */
            animation: fadeInUp 0.6s ease-out;
            padding: 0 8px;
        }

        .page-title {
            font-size: 28px;
            /* Increased from 24px */
            font-weight: 700;
            color: var(--dark);
            margin: 0 0 8px;
            /* Increased from 6px */
            background: linear-gradient(135deg, var(--primary) 0%, var(--primary-dark) 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            padding-bottom: 8px;
        }

        .page-subtitle {
            font-size: 14px;
            /* Increased from 13px */
            color: var(--gray);
            margin: 0 0 20px;
            /* Increased from 16px */
            animation: fadeIn 0.8s ease-out 0.3s both;
            padding-left: 4px;
        }

        /* Info Alert */
        .info-alert {
            background: #E8F4FD;
            border: 2px solid #B3D9FF;
            /* Increased from 1px */
            border-radius: var(--radius);
            padding: 16px 20px;
            /* Increased from 12px */
            margin-bottom: 24px;
            /* Increased from 20px */
            display: flex;
            align-items: flex-start;
            gap: 12px;
            /* Increased from 10px */
            animation: fadeInUp 0.5s ease-out 0.6s both;
            transition: var(--transition);
        }

        .info-alert:hover {
            transform: translateY(-2px);
            /* Increased from -1px */
            box-shadow: 0 6px 20px rgba(0, 123, 255, 0.2);
            /* Enhanced shadow */
        }

        .info-alert i {
            color: #007BFF;
            font-size: 16px;
            /* Increased from 14px */
            flex-shrink: 0;
            margin-top: 2px;
            animation: bounce 2s infinite;
        }

        .info-alert p {
            margin: 0;
            font-size: 13px;
            /* Increased from 12px */
            color: #0C5460;
            line-height: 1.5;
            padding: 2px 0;
        }

        /* Talent Header */
        .talent-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 24px;
            /* Increased from 20px */
            padding-bottom: 16px;
            /* Increased from 12px */
            border-bottom: 2px solid var(--border);
            /* Increased from 1px */
            animation: fadeInUp 0.6s ease-out 0.7s both;
        }

        .talent-title {
            font-size: 18px;
            /* Increased from 16px */
            font-weight: 600;
            color: var(--dark);
            margin: 0;
            position: relative;
            display: inline-block;
            padding-right: 16px;
        }

        .talent-title::after {
            content: '';
            position: absolute;
            bottom: -16px;
            /* Adjusted for border thickness */
            left: 0;
            width: 50px;
            /* Increased from 40px */
            height: 3px;
            background: var(--primary);
            border-radius: 2px;
            animation: slideInRight 0.6s ease-out 0.9s both;
        }

        .talent-count {
            font-size: 13px;
            /* Increased from 12px */
            color: var(--gray);
            font-weight: 500;
            background: var(--primary-light);
            padding: 6px 14px;
            /* Increased from 4px 10px */
            border-radius: 24px;
            /* Increased from 20px */
            animation: fadeIn 0.6s ease-out 0.8s both;
        }

        .talent-count:hover {
            transform: scale(1.08);
            /* Increased from 1.05 */
            padding: 6px 16px;
            /* Increased padding on hover */
        }

        /* Talent Grid */
        .talent-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
            /* Increased from 300px */
            gap: 20px;
            /* Increased from 16px */
        }

        /* Empty State */
        .empty-state {
            text-align: center;
            padding: 80px 40px;
            /* Increased from 60px 20px */
            background: white;
            border: 2px solid var(--border);
            /* Increased from 1px */
            border-radius: var(--radius);
            animation: fadeIn 0.8s ease-out;
        }

        .empty-state i {
            font-size: 72px;
            /* Increased from 64px */
            color: #DEE2E6;
            margin-bottom: 24px;
            /* Increased from 20px */
            animation: float 4s ease-in-out infinite;
        }

        .empty-state h3 {
            font-size: 20px;
            /* Increased from 18px */
            font-weight: 600;
            color: var(--dark);
            margin: 0 0 16px;
            /* Increased from 12px */
            animation: fadeInUp 0.6s ease-out 0.2s both;
        }

        .empty-state p {
            font-size: 15px;
            /* Increased from 14px */
            color: var(--gray);
            margin: 0;
            animation: fadeInUp 0.6s ease-out 0.4s both;
        }

        /* Freelancer Card */
        .freelancer-card {
            background: white;
            border: 2px solid var(--border);
            /* Increased from 1px */
            border-radius: var(--radius);
            padding: 20px;
            /* Increased from 16px */
            transition: var(--transition);
            opacity: 0;
            transform: translateY(20px);
            animation: fadeInUp 0.5s ease-out forwards;
            position: relative;
            overflow: hidden;
        }

        .freelancer-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(229, 71, 20, 0.05), transparent);
            /* Increased opacity */
            transition: left 0.6s ease;
        }

        .freelancer-card:hover::before {
            left: 100%;
        }

        .freelancer-card:hover {
            border-color: var(--primary);
            box-shadow: 0 12px 30px rgba(229, 71, 20, 0.15);
            /* Enhanced shadow */
            transform: translateY(-6px);
            /* Increased from -4px */
            border-width: 3px;
            /* Added border width increase */
        }

        .card-header {
            display: flex;
            align-items: flex-start;
            margin-bottom: 18px;
            /* Increased from 14px */
        }

        .freelancer-avatar {
            width: 80px;
            /* Increased from 70px */
            height: 80px;
            /* Increased from 70px */
            border-radius: 50%;
            object-fit: cover;
            border: 3px solid var(--primary-light);
            margin-right: 18px;
            /* Increased from 14px */
            transition: var(--transition);
        }

        .freelancer-avatar:hover {
            transform: scale(1.08);
            /* Increased from 1.05 */
            border-color: var(--primary);
        }

        .freelancer-info {
            flex: 1;
        }

        .freelancer-name {
            font-size: 18px;
            /* Increased from 16px */
            font-weight: 600;
            color: var(--dark);
            margin: 0 0 6px;
            /* Increased from 4px */
            transition: var(--transition);
        }

        .freelancer-name a {
            color: inherit;
            text-decoration: none;
            position: relative;
            padding: 2px 0;
        }

        .freelancer-name a::after {
            content: '';
            position: absolute;
            bottom: -2px;
            left: 0;
            width: 0;
            height: 2px;
            background: var(--primary);
            transition: width 0.3s ease;
        }

        .freelancer-name a:hover::after {
            width: 100%;
        }

        .freelancer-name a:hover {
            color: var(--primary);
            padding-left: 4px;
        }

        .status-badges {
            display: flex;
            gap: 8px;
            /* Increased from 6px */
            margin-bottom: 12px;
            /* Increased from 8px */
        }

        .status-badge {
            font-size: 11px;
            /* Increased from 10px */
            padding: 4px 10px;
            /* Increased from 3px 8px */
            border-radius: 14px;
            /* Increased from 12px */
            font-weight: 600;
            transition: var(--transition);
        }

        .status-badge:hover {
            transform: scale(1.12);
            /* Increased from 1.1 */
        }

        .badge-verified {
            background: #D4EDDA;
            color: #155724;
            animation: pulseGreen 2s infinite;
        }

        .badge-rating {
            background: #FFF3CD;
            color: #856404;
        }

        .freelancer-location {
            font-size: 13px;
            /* Increased from 12px */
            color: var(--gray);
            margin-bottom: 12px;
            /* Increased from 8px */
            display: flex;
            align-items: center;
            gap: 6px;
            /* Increased from 4px */
        }

        .freelancer-location i {
            color: var(--primary);
            font-size: 12px;
            /* Increased from 11px */
        }

        /* Skills Section */
        .skills-section {
            margin-bottom: 18px;
            /* Increased from 14px */
        }

        .skills-label {
            font-size: 12px;
            /* Increased from 11px */
            font-weight: 600;
            color: var(--gray);
            text-transform: uppercase;
            letter-spacing: 0.6px;
            /* Increased from 0.5px */
            margin-bottom: 8px;
            /* Increased from 6px */
            display: flex;
            align-items: center;
            gap: 6px;
            /* Increased from 4px */
        }

        .skills-label i {
            color: var(--primary);
            font-size: 12px;
            /* Increased from 10px */
        }

        .skills-list {
            display: flex;
            flex-wrap: wrap;
            gap: 6px;
            /* Increased from 4px */
            margin-bottom: 8px;
            /* Increased from 6px */
        }

        .skill-badge {
            font-size: 11px;
            /* Increased from 10px */
            padding: 4px 10px;
            /* Increased from 3px 8px */
            background: var(--primary-light);
            color: var(--primary-dark);
            border-radius: 14px;
            /* Increased from 12px */
            font-weight: 600;
            transition: var(--transition);
        }

        .skill-badge:hover {
            background: var(--primary);
            color: white;
            transform: translateY(-3px);
            /* Increased from -2px */
            padding: 4px 12px;
            /* Increased padding on hover */
        }

        .experience-summary {
            font-size: 12px;
            /* Increased from 11px */
            color: var(--gray);
            display: flex;
            align-items: center;
            gap: 8px;
            /* Increased from 6px */
        }

        .experience-summary i {
            color: var(--primary);
            font-size: 12px;
            /* Increased from 10px */
        }

        /* Ratings Section */
        .ratings-section {
            margin-bottom: 20px;
            /* Increased from 16px */
        }

        .stars {
            display: flex;
            align-items: center;
            gap: 3px;
            /* Increased from 2px */
            margin-bottom: 6px;
            /* Increased from 4px */
        }

        .star {
            color: #FFC107;
            font-size: 14px;
            /* Increased from 12px */
            transition: var(--transition);
        }

        .star:hover {
            transform: scale(1.25);
            /* Increased from 1.2 */
        }

        .rating-details {
            display: flex;
            align-items: center;
            gap: 12px;
            /* Increased from 8px */
            font-size: 12px;
            /* Increased from 11px */
            color: var(--gray);
        }

        .rating-link {
            color: var(--primary);
            text-decoration: none;
            font-weight: 600;
            transition: var(--transition);
            padding: 2px 4px;
        }

        .rating-link:hover {
            color: var(--primary-dark);
            text-decoration: underline;
            padding-left: 6px;
        }

        /* Actions Section */
        .actions-section {
            display: flex;
            gap: 12px;
            /* Increased from 8px */
            padding-top: 18px;
            /* Increased from 14px */
            border-top: 2px solid var(--border);
            /* Increased from 1px */
        }

        .action-btn {
            flex: 1;
            padding: 10px 12px;
            /* Increased from 8px 10px */
            border-radius: var(--radius);
            font-size: 13px;
            /* Increased from 12px */
            font-weight: 600;
            cursor: pointer;
            text-decoration: none;
            transition: var(--transition);
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
            /* Increased from 6px */
            position: relative;
            overflow: hidden;
        }

        .action-btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.25), transparent);
            /* Increased opacity */
            transition: left 0.5s ease;
        }

        .action-btn:hover::before {
            left: 100%;
        }

        .btn-view {
            background: white;
            color: var(--primary);
            border: 2px solid var(--primary);
            /* Increased from 1px */
        }

        .btn-view:hover {
            background: var(--primary);
            color: white;
            transform: translateY(-3px);
            /* Increased from -2px */
            box-shadow: 0 6px 20px rgba(229, 71, 20, 0.25);
            /* Enhanced shadow */
            padding: 10px 16px;
            /* Increased padding on hover */
        }

        .btn-message {
            background: white;
            color: #28A745;
            border: 2px solid #28A745;
            /* Increased from 1px */
        }

        .btn-message:hover {
            background: #28A745;
            color: white;
            transform: translateY(-3px);
            /* Increased from -2px */
            box-shadow: 0 6px 20px rgba(40, 167, 69, 0.25);
            /* Enhanced shadow */
            padding: 10px 16px;
            /* Increased padding on hover */
        }

        .btn-invite {
            background: var(--primary);
            color: white;
            border: 2px solid var(--primary);
            /* Increased from 1px */
        }

        .btn-invite:hover {
            background: var(--primary-dark);
            transform: translateY(-3px);
            /* Increased from -2px */
            box-shadow: 0 6px 20px rgba(229, 71, 20, 0.4);
            /* Enhanced shadow */
            padding: 10px 16px;
            /* Increased padding on hover */
        }

        .btn-invite:disabled {
            opacity: 0.5;
            cursor: not-allowed;
        }

        .btn-invite:disabled:hover {
            transform: none;
            box-shadow: none;
            padding: 10px 12px;
            /* Reset padding on hover */
        }

        /* Responsive */
        @media (max-width: 1200px) {
            .talent-grid {
                grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
                gap: 16px;
            }

            .sidebar {
                width: 260px;
                padding: 16px;
            }
        }

        @media (max-width: 992px) {
            .find-talent-container {
                flex-direction: column;
            }

            .sidebar {
                width: 100%;
                max-height: 420px;
                /* Increased from 400px */
                border-right: none;
                border-bottom: 2px solid var(--border);
                /* Increased from 1px */
                padding: 16px 20px;
            }

            .main-content {
                padding: 20px;
            }

            .talent-grid {
                grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
                /* Increased from 250px */
                gap: 16px;
            }

            .actions-section {
                flex-direction: column;
            }
        }

        @media (max-width: 768px) {
            .navbar-nav {
                padding: 12px 0;
                /* Increased from 10px */
            }

            .navbar-nav .nav-item {
                margin: 4px 0;
                /* Increased from 2px */
            }

            .talent-grid {
                grid-template-columns: 1fr;
                gap: 16px;
            }

            .page-title {
                font-size: 24px;
                /* Increased from 20px */
            }

            .card-header {
                flex-direction: column;
                align-items: center;
                text-align: center;
                margin-bottom: 16px;
            }

            .freelancer-avatar {
                margin-right: 0;
                margin-bottom: 16px;
                /* Increased from 12px */
            }

            .freelancer-name {
                font-size: 16px;
            }

            .actions-section {
                gap: 8px;
            }

            .action-btn {
                padding: 8px 12px;
                font-size: 12px;
            }
        }

        @media (max-width: 480px) {
            .navbar-brand {
                font-size: 18px;
                padding-left: 12px;
            }

            .user-name {
                display: none;
            }

            .logout-btn {
                padding: 6px 12px;
                font-size: 12px;
            }

            .sidebar {
                padding: 12px 16px;
            }

            .filter-input,
            .filter-select {
                padding: 8px 10px;
                font-size: 13px;
            }

            .filter-btn,
            .clear-btn {
                padding: 10px 12px;
                font-size: 13px;
            }

            .main-content {
                padding: 16px;
            }

            .page-title {
                font-size: 22px;
            }
        }

        /* Animations */
        @keyframes fadeIn {
            from {
                opacity: 0;
            }

            to {
                opacity: 1;
            }
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(24px);
                /* Increased from 20px */
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes slideDown {
            from {
                opacity: 0;
                transform: translateY(-24px);
                /* Increased from -20px */
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes slideInLeft {
            from {
                opacity: 0;
                transform: translateX(-24px);
                /* Increased from -20px */
            }

            to {
                opacity: 1;
                transform: translateX(0);
            }
        }

        @keyframes slideInRight {
            from {
                opacity: 0;
                transform: translateX(24px);
                /* Increased from 20px */
            }

            to {
                opacity: 1;
                transform: translateX(0);
            }
        }

        @keyframes float {

            0%,
            100% {
                transform: translateY(0);
            }

            50% {
                transform: translateY(-6px);
            }

            /* Increased from -5px */
        }

        @keyframes bounce {

            0%,
            20%,
            50%,
            80%,
            100% {
                transform: translateY(0);
            }

            40% {
                transform: translateY(-4px);
            }

            /* Increased from -3px */
            60% {
                transform: translateY(-2px);
            }
        }

        @keyframes pulseGreen {
            0% {
                box-shadow: 0 0 0 0 rgba(40, 167, 69, 0.4);
            }

            70% {
                box-shadow: 0 0 0 6px rgba(40, 167, 69, 0);
            }

            /* Increased from 4px */
            100% {
                box-shadow: 0 0 0 0 rgba(40, 167, 69, 0);
            }
        }

        /* Staggered animation for freelancer cards */
        .freelancer-card:nth-child(1) {
            animation-delay: 0.1s;
        }

        .freelancer-card:nth-child(2) {
            animation-delay: 0.2s;
        }

        .freelancer-card:nth-child(3) {
            animation-delay: 0.3s;
        }

        .freelancer-card:nth-child(4) {
            animation-delay: 0.4s;
        }

        .freelancer-card:nth-child(5) {
            animation-delay: 0.5s;
        }

        .freelancer-card:nth-child(6) {
            animation-delay: 0.6s;
        }

        .freelancer-card:nth-child(7) {
            animation-delay: 0.7s;
        }

        .freelancer-card:nth-child(8) {
            animation-delay: 0.8s;
        }

        .freelancer-card:nth-child(9) {
            animation-delay: 0.9s;
        }

        .freelancer-card:nth-child(10) {
            animation-delay: 1s;
        }

        /* Smooth scrolling */
        html {
            scroll-behavior: smooth;
        }

        /* Custom scrollbar for sidebar */
        .sidebar::-webkit-scrollbar {
            width: 6px;
        }

        .sidebar::-webkit-scrollbar-track {
            background: #f1f1f1;
            border-radius: 3px;
        }

        .sidebar::-webkit-scrollbar-thumb {
            background: var(--primary);
            border-radius: 3px;
        }

        .sidebar::-webkit-scrollbar-thumb:hover {
            background: var(--primary-dark);
        }
    </style>
</head>

<body>
    <!-- Header -->
    <nav class="navbar navbar-expand-lg">
        <div class="container-fluid">
            <a class="navbar-brand" th:href="@{/client-dashboard/}">Jobify</a>

            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent"
                aria-controls="navbarContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item">
                        <a class="nav-link" th:href="@{/client-dashboard/}">
                            <i class="fas fa-home fa-sm"></i> Dashboard
                        </a>
                    </li>
                    <li class="nav-item" th:if="${isVerified}">
                        <a class="nav-link" th:href="@{/dashboard/add}">
                            <i class="fas fa-file-circle-plus fa-sm"></i> Post Job
                        </a>
                    </li>
                    <li class="nav-item active">
                        <a class="nav-link" th:href="@{/find-talent/}">
                            <i class="fas fa-users fa-sm"></i> Find Talent
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" th:href="@{/client-dashboard/proposals}">
                            <i class="fas fa-file-alt fa-sm"></i> Proposals
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" th:href="@{/client-dashboard/projects}">
                            <i class="fas fa-briefcase fa-sm"></i> Projects
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" th:href="@{/chat/}">
                            <i class="fas fa-comments fa-sm"></i> Messages
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" th:href="@{/recruiter-profile/}">
                            <i class="fas fa-user-cog fa-sm"></i> Profile
                        </a>
                    </li>
                </ul>

                <!-- User Info -->
                <div class="d-flex align-items-center">
                    <img class="user-avatar rounded-circle me-2" th:if="${user.photosImagePath != null}"
                        th:src="${user.photosImagePath}" alt="Profile" />

                    <span class="user-name me-3">
                        <span th:if="${user.firstName != null and user.lastName != null}"
                            th:text="${user.firstName + ' ' + user.lastName}"></span>
                    </span>

                    <a class="logout-btn btn btn-sm" th:href="@{/logout}">
                        <i class="fas fa-sign-out-alt fa-sm"></i> Logout
                    </a>
                </div>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <div class="find-talent-container">
        <!-- Sidebar Filters -->
        <aside class="sidebar">
            <div class="sidebar-header">Filter Freelancers</div>

            <form id="filterForm" th:action="@{/find-talent/}" method="get">
                <!-- Search -->
                <div class="filter-group">
                    <div class="filter-label">
                        <i class="fas fa-search"></i> Search
                    </div>
                    <input type="text" class="filter-input" name="search" placeholder="Name, location..."
                        th:value="${search}">
                </div>

                <!-- Skill -->
                <div class="filter-group">
                    <div class="filter-label">
                        <i class="fas fa-code"></i> Skill
                    </div>
                    <input type="text" class="filter-input" name="skill" placeholder="e.g., Java, Python..."
                        th:value="${skill}">
                </div>

                <!-- Location -->
                <div class="filter-group">
                    <div class="filter-label">
                        <i class="fas fa-map-marker-alt"></i> Location
                    </div>
                    <input type="text" class="filter-input" name="location" placeholder="City, State, Country"
                        th:value="${location}">
                </div>

                <!-- Experience Level -->
                <div class="filter-group">
                    <div class="filter-label">
                        <i class="fas fa-chart-line"></i> Experience Level
                    </div>
                    <select class="filter-select" name="experienceLevel">
                        <option value="">All Levels</option>
                        <option value="Beginner" th:selected="${experienceLevel == 'Beginner'}">Beginner</option>
                        <option value="Intermediate" th:selected="${experienceLevel == 'Intermediate'}">Intermediate
                        </option>
                        <option value="Advanced" th:selected="${experienceLevel == 'Advanced'}">Advanced</option>
                        <option value="Expert" th:selected="${experienceLevel == 'Expert'}">Expert</option>
                    </select>
                </div>

                <!-- Minimum Rating -->
                <div class="filter-group">
                    <div class="filter-label">
                        <i class="fas fa-star"></i> Minimum Rating
                    </div>
                    <select class="filter-select" name="minRating">
                        <option value="">All Ratings</option>
                        <option value="4.5" th:selected="${minRating == 4.5}">4.5+ Stars</option>
                        <option value="4.0" th:selected="${minRating == 4.0}">4.0+ Stars</option>
                        <option value="3.5" th:selected="${minRating == 3.5}">3.5+ Stars</option>
                        <option value="3.0" th:selected="${minRating == 3.0}">3.0+ Stars</option>
                    </select>
                </div>

                <button type="submit" class="filter-btn">
                    <i class="fas fa-filter"></i> Apply Filters
                </button>
                <a th:href="@{/find-talent/}" class="clear-btn">
                    <i class="fas fa-times"></i> Clear Filters
                </a>
            </form>
        </aside>

        <!-- Main Content Area -->
        <main class="main-content">
            <!-- Page Header -->
            <div class="page-header">
                <h1 class="page-title">Find Talent</h1>
                <p class="page-subtitle">Browse verified freelancers and invite them to work on your projects.</p>
            </div>

            <!-- Info Alert -->
            <div class="info-alert">
                <i class="fas fa-info-circle"></i>
                <p>Only verified freelancer accounts are displayed to ensure reliability.</p>
            </div>

            <!-- Talent Header -->
            <div class="talent-header">
                <h2 class="talent-title">Available Freelancers</h2>
                <span class="talent-count" th:if="${freelancers != null and not freelancers.isEmpty()}">
                    [[${freelancers.size()}]] freelancer(s)
                </span>
            </div>

            <!-- Empty State -->
            <div th:if="${freelancers == null or freelancers.isEmpty()}" class="empty-state">
                <i class="fas fa-users"></i>
                <h3>No Freelancers Found</h3>
                <p>No freelancers match your search criteria. Try adjusting your filters.</p>
            </div>

            <!-- Talent Grid -->
            <div class="talent-grid" th:if="${freelancers != null and not freelancers.isEmpty()}">
                <div th:each="freelancer : ${freelancers}" class="freelancer-card">
                    <div class="card-header">
                        <img class="freelancer-avatar" th:if="${freelancer.photosImagePath != null}"
                            th:src="${freelancer.photosImagePath}" th:unless="${freelancer.photosImagePath != null}"
                            src="data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 24 24' fill='%23E54714'%3E%3Cpath d='M12 12c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm0 2c-2.67 0-8 1.34-8 4v2h16v-2c0-2.66-5.33-4-8-4z'/%3E%3C/svg%3E"
                            alt="Avatar">
                        <div class="freelancer-info">
                            <h3 class="freelancer-name">
                                <a th:href="@{/job-seeker-profile/view/{id}(id=${freelancer.userAccountId})}">
                                    [[${freelancer.firstName}]] [[${freelancer.lastName}]]
                                </a>
                            </h3>
                            <div class="status-badges">
                                <span class="status-badge badge-verified">
                                    <i class="fas fa-check-circle"></i> Verified
                                </span>
                                <span
                                    th:if="${freelancerRatings != null and freelancerRatings.get(freelancer.userAccountId) != null and freelancerRatings.get(freelancer.userAccountId) > 0}"
                                    class="status-badge badge-rating">
                                    <i class="fas fa-star"></i>
                                    [[${#numbers.formatDecimal(freelancerRatings.get(freelancer.userAccountId), 1,
                                    'POINT', 1, 'POINT')}]]
                                </span>
                            </div>
                            <div class="freelancer-location"
                                th:if="${freelancer.city != null or freelancer.state != null}">
                                <i class="fas fa-map-marker-alt"></i>
                                [[${freelancer.city}]][[${freelancer.city != null and freelancer.state != null ? ', ' :
                                ''}]][[${freelancer.state}]][[${freelancer.city != null or freelancer.state != null ? ',
                                ' : ''}]][[${freelancer.country}]]
                            </div>
                        </div>
                    </div>

                    <!-- Skills -->
                    <div class="skills-section"
                        th:if="${freelancer.skills != null and not freelancer.skills.isEmpty()}">
                        <div class="skills-label">
                            <i class="fas fa-code"></i> Skills
                        </div>
                        <div class="skills-list">
                            <span th:each="skill : ${freelancer.skills}" class="skill-badge">
                                [[${skill.name}]]
                            </span>
                        </div>
                        <div class="experience-summary">
                            <i class="fas fa-briefcase"></i>
                            [[${freelancer.skills.size()}]] skills
                            <span th:if="${freelancer.employmentType != null}">
                                | [[${freelancer.employmentType}]]
                            </span>
                        </div>
                    </div>

                    <!-- Ratings -->
                    <div class="ratings-section"
                        th:if="${freelancerRatings != null and freelancerRatings.get(freelancer.userAccountId) != null and freelancerRatings.get(freelancer.userAccountId) > 0}">
                        <div class="stars">
                            <span th:each="i : ${#numbers.sequence(1, 5)}">
                                <i class="fas fa-star star"
                                    th:classappend="${i <= freelancerRatings.get(freelancer.userAccountId)} ? 'fas' : 'far'"></i>
                            </span>
                        </div>
                        <div class="rating-details">
                            <span>[[${#numbers.formatDecimal(freelancerRatings.get(freelancer.userAccountId), 1,
                                'POINT', 1, 'POINT')}]]</span>
                            <span
                                th:if="${freelancerRatingCounts != null and freelancerRatingCounts.get(freelancer.userAccountId) > 0}">
                                ([[${freelancerRatingCounts.get(freelancer.userAccountId)}]] reviews)
                            </span>
                            <a th:href="@{/ratings/view/{id}(id=${freelancer.userAccountId})}" class="rating-link">
                                View Reviews
                            </a>
                        </div>
                    </div>

                    <!-- Actions -->
                    <div class="actions-section">
                        <a th:href="@{/job-seeker-profile/view/{id}(id=${freelancer.userAccountId})}"
                            class="action-btn btn-view">
                            <i class="fas fa-eye"></i> View
                        </a>
                        <div class="card-actions">
                            <!-- Show "Sign in to Contact" banner for non-logged-in users -->
                            <div th:if="${!isLoggedIn}" class="signin-banner">
                                <i class="fas fa-sign-in-alt"></i>
                                <a th:href="@{/login}" class="signin-link">Sign in to Contact</a>
                            </div>
                            <!-- Show contact buttons for logged-in users -->
                            <div th:if="${isLoggedIn}">
                                <a th:href="@{/chat/{userId}(userId=${freelancer.userAccountId})}"
                                    class="action-btn btn-message">
                                    <i class="fas fa-comments"></i> Message
                                </a>
                                <button class="action-btn btn-invite" th:disabled="${not isVerified}" th:attr="data-freelancer-id=${freelancer.userAccountId},
                                             data-freelancer-name=${freelancer.firstName + ' ' + freelancer.lastName}"
                                    onclick="inviteFreelancer(this)">
                                    <i class="fas fa-user-plus"></i> Invite
                                </button>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </main>
    </div>

    <!-- Invite Modal -->
    <div class="modal fade" id="inviteModal" tabindex="-1" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content">
                <div class="modal-header" style="background: var(--primary); color: white;">
                    <h5 class="modal-title">Invite Freelancer</h5>
                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body" style="padding: 24px;">
                    <p><strong>Note:</strong> Invitation functionality can be integrated with job posting or direct
                        messaging.</p>
                    <p><strong>Freelancer:</strong> <span id="modalFreelancerName" class="fw-bold"></span></p>
                    <div class="alert alert-info mt-3" style="border: 2px solid #B3D9FF; padding: 16px;">
                        <i class="fas fa-info-circle me-2"></i>
                        You can invite this freelancer by messaging them or posting a job and inviting them to apply.
                    </div>
                </div>
                <div class="modal-footer" style="padding: 20px 24px;">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal"
                        style="padding: 10px 20px;">Close</button>
                    <a id="inviteMessageLink" href="#" class="btn btn-primary" style="padding: 10px 24px;">
                        <i class="fas fa-comments me-2"></i> Send Message
                    </a>
                </div>
            </div>
        </div>
    </div>

    <script th:src="@{/webjars/jquery/jquery.min.js}"></script>
    <script th:src="@{/webjars/bootstrap/js/bootstrap.bundle.min.js}"></script>

    <script>
        /*<![CDATA[*/
        function inviteFreelancer(button) {
            var freelancerId = button.getAttribute('data-freelancer-id');
            var freelancerName = button.getAttribute('data-freelancer-name');

            document.getElementById('modalFreelancerName').textContent = freelancerName;
            document.getElementById('inviteMessageLink').href = '/chat/' + freelancerId;

            var inviteModal = new bootstrap.Modal(document.getElementById('inviteModal'));
            inviteModal.show();
        }

        // Enhanced hover effects with better animation
        $(document).ready(function () {
            $('.skill-badge, .status-badge').hover(
                function () {
                    $(this).css('transform', 'translateY(-3px)');
                },
                function () {
                    $(this).css('transform', 'translateY(0)');
                }
            );

            $('.star').hover(
                function () {
                    $(this).css('transform', 'scale(1.25)');
                },
                function () {
                    $(this).css('transform', 'scale(1)');
                }
            );

            $('.action-btn').hover(
                function () {
                    $(this).css('transform', 'translateY(-3px)');
                },
                function () {
                    $(this).css('transform', 'translateY(0)');
                }
            );

            // Filter form animation
            $('#filterForm').on('submit', function () {
                $('.filter-btn').css('transform', 'scale(0.95)');
                setTimeout(function () {
                    $('.filter-btn').css('transform', 'scale(1)');
                }, 200);
            });

            // Smooth scrolling for anchor links
            $('a[href^="#"]').on('click', function (event) {
                if (this.hash !== "") {
                    event.preventDefault();
                    const hash = this.hash;
                    $('html, body').animate({
                        scrollTop: $(hash).offset().top - 80
                    }, 800);
                }
            });

            // Add loading animation to filter button
            $('.filter-btn').on('click', function () {
                const $btn = $(this);
                $btn.css('transform', 'scale(0.95)');
                setTimeout(function () {
                    $btn.css('transform', 'scale(1)');
                }, 200);
            });
        });
        /*]]>*/
    </script>
</body>

</html>